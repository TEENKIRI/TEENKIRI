server:
  port: 8081

spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3307/teenkiri
    username: root
    password: 1234

  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: update
    show-sql: true

  servlet:
    multipart:
      max-file-size: 5120MB  # 최대 5GB
      max-request-size: 5120MB  # 최대 5GB

  mail:
    host: smtp.gmail.com
    port: 587
    username: dldpsk122212@gmail.com
    password: phlj naoz tbmo aidz
    properties:
      mail:
        transport:
          protocol: smtp
        smtp:
          auth: true
          starttls:
            enable: true
        debug: true

  redis:
    host: localhost
    port: 6379
    # password: 1234

  jwt:
    secret: secret_key
    expiration: 86400000  # 1 day in milliseconds

